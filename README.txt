Neo cursor update v30 – Simple high-contrast star

Put the contents of this ZIP at the root of your static site.
index.html must be at the top level.

This version goes back to a simple, solid, high-contrast 4-pointed star
so the shape is actually visible and not lost in glow or gradients.
